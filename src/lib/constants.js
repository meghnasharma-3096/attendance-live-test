// The single professor login's timetable_slots.professor_identifier value. This has drifted
// more than once (prof_dtai -> prof -> prof_dtai_itc) with different files catching up at
// different times — kept in exactly one place now so a future rename is a one-line change.
export const PROFESSOR_IDENTIFIER = 'prof_dtai_itc'
